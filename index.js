// Define divide function expression
const divide = function() {
    return 2000 / 100;
  };
  
  const square = (num) => num * num;
  
  // add arrow function
  const add = (num1, num2) => num1 + num2;
  
  // output the functions
  module.exports = {
    divide,
    square,
    add,
  };
  